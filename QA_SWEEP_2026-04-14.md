# QA Sweep — 2026-04-14

## Escopo
Varredura funcional e técnica do painel (`index.html`) com foco em:
- Carga base do painel
- Consolidação mensal (JAN/FEV/MAR/ABR)
- Links de projeto (coluna W)
- Bloco "Resumo do Projeto" (fases/tasks/marcos/progresso)

## Evidências coletadas
- Inspeção estática do código JS/HTML em `index.html`.
- Inspeção do dataset embutido (`DATA = JSON.parse(atob(...))`).
- Evidência visual enviada: cards com "Resumo indisponível".

## Resultado geral
**STATUS: PARCIALMENTE APROVADO (camada base OK, resumo reprovado por falta de origem estruturada).**

---

## Achados

### 1) Camada base carrega e renderiza
**Severidade:** Baixa (informativo)  
**Status:** OK

- A base é normalizada em `prepararProjetosBase` e consolidada em `carregarBaseProjetos`.
- O fluxo de finalização usa `DATA.projetos = baseInfo.projetos`, sem depender do resumo avançado.

Impacto: reduz risco de regressão de "Carregando..." infinito.

---

### 2) Consolidação mensal incompleta na prática (ABR sem dados)
**Severidade:** Média  
**Status:** Atenção

- O código fixa abas `JAN/FEV/MAR/ABR` em `ABAS_GID_FIXAS` e na camada base.
- No dataset embutido atual há apenas:
  - JAN: 10
  - FEV: 23
  - MAR: 36
  - ABR: 0

Impacto: ABR pode aparecer sem dados quando a origem remota não retornar linhas.

---

### 3) Links de projeto: implementação presente, validação remota pendente
**Severidade:** Média  
**Status:** Parcial

- O link é aplicado por `applyProjectLinks` com prioridade para campo do projeto e fallback por mapa de planilha.
- Renderização do botão `Abrir Projeto` está implementada na linha e no expandido.
- Em ambiente local desta varredura não foi possível validar leitura remota `gviz` (histórico de 403 em execuções anteriores).

Impacto: funcionalidade depende de resposta externa da planilha.

---

### 4) Resumo do Projeto (fases/tasks/marcos) indisponível por ausência de payload
**Severidade:** Alta  
**Status:** Reprovado funcional (mas comportamento atual está coerente)

- O resumo só é montado se `temResumoEstrutural(p)` detectar estrutura.
- O dataset atual não contém nenhum dos campos esperados (`itens_fase`, `tasks`, `marcos_*`, etc.).
- Resultado esperado pelo código: mensagem "Resumo indisponível para este projeto (...)".

Impacto: o painel não consegue mostrar fases/tasks/marcos/progresso real sem origem estruturada.

---

## Conclusão QA (objetiva)
1. **Estabilidade do painel:** OK.
2. **Consolidação por mês:** Parcial (ABR depende totalmente da origem remota; local está zerado).
3. **Links:** implementação OK, validação E2E remota pendente.
4. **Resumo avançado:** **não há dados de origem suficientes**; sem payload estruturado não existe como calcular com qualidade.

---

## Ação recomendada (sem remendo)
Para aprovar definitivamente fases/tasks/marcos/progresso:
1. Garantir que a origem (Apps Script/planilha/API) entregue por projeto os campos estruturados:
   - `itens_fase` (ou equivalente)
   - status por item
   - classificação de milestone
2. Só então revalidar o cálculo no front.

Sem isso, qualquer número de resumo será inferência fraca.
